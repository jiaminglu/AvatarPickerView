# Android-AvatarPickerView
Usage:
  1. ```setImageURI```/```setImageDrawable``` etc. (AvatarPickerView extends ImageView)
  2. Drag it.
  3. ```Bitmap bitmap = avatarPickerView.getClippedBitmap(64);``` // Assume the max avatar size to be 64.
